name: Continuous Deployment - Publish Libraries

on:
  release:
    types: [published]
  workflow_dispatch:
    dry-run:
      description: Whether to skip the publication of packages to the repositories
      type: boolean

jobs:
  compute-version:
    name: Compute version
    uses: ./.github/workflows/reusable-compute-version.yml
    with:
      build-name: pr

  build-publish:
    runs-on: ubuntu-latest
    needs: compute-version
    
    permissions:
      actions: write
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install .NET 10.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Build
        run: dotnet build --configuration Release --property Version=${{ needs.compute-version.outputs.version }}

      - name: Test
        run: dotnet test --configuration Release --no-build
        
      - name: Pack
        run: dotnet pack --output nuget/ --property Version=${{ needs.compute-version.outputs.version }}
      
      - name: Upload binaries as artifact
        uses: actions/upload-artifact@v4
        with:
          name: libraries
          path: nuget/*.nupkg
          if-no-files-found: error
          
      - name: Publish nugets - Github
        if: ${{ !inputs.dry-run }}
        run: |
          foreach($File in (Get-ChildItem "nuget/" -Recurse -Include *.nupkg)) {
              dotnet nuget push $File --source github --api-key ${{ secrets.GITHUB_TOKEN }}
          }

      - name: Publish nugets - Nuget
        if: ${{ !inputs.dry-run }}
        run: |
          foreach($File in (Get-ChildItem "nuget/" -Recurse -Include *.nupkg)) {
              dotnet nuget push $File --source https://api.nuget.org/v3/index.json --api-key ${{ secrets.NUGET_TOKEN }}